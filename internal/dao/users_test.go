package dao

import (
	"testing"
)

func TestFindByID(t *testing.T) {
	/*model := &models.User{
		FirstName: "test",
	}

	mockUserDao := &daomock.UserDaoMock{}
	mockUserDao.On("FindByID", uint(10)).Return(model, nil)

	userDao := NewUser(nil)
	//happy case
	user, _ := userDao.FindByID(uint(10))

	assert.Equal(t, user.ID, model.ID)
	assert.Equal(t, user.FirstName, model.FirstName)*/
}
